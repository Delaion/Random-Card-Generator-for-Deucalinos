/* eslint-disable */
import "bootstrap";
import "./style.css";

import "./assets/img/rigo-baby.jpg";
import "./assets/img/4geeks.ico";

window.onload = function() {
  let cardElement= document.getElementById("icon");
  let cardElement= document.getElementById (number);
  cardElement.innerHTML= generateCard ();
  console.log(generateCard);
};
function generateCard(){
  var icon= [♦,♥,♠,♣];
  var number=[2,3,4,5,6,7,8,9,10];
  let randomIcon = icon[Math.floor(Math.random() * icon.length)];
  let randomNumber = number[Math.floor(Math.random() * number.length)];
  let card=window.onload;
  return window.onload;
}
console.log(window.onload);
